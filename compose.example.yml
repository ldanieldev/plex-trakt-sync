services:
  plextrakt:
    image: ghcr.io/OWNER/plextrakt:latest   # replace OWNER after first push
    container_name: plextrakt
    init: true
    environment:
      PLEX_URL: "http://plex:32400"
      PLEX_TOKEN: "${PLEX_TOKEN}"
      # PLEX_EXCLUDE_LIBRARIES: "Other Videos"
      TRAKT_CLIENT_ID: "${TRAKT_CLIENT_ID}"
      TRAKT_CLIENT_SECRET: "${TRAKT_CLIENT_SECRET}"
      SYNC_INTERVAL: "6h"
    # host dir must be owned by uid 1000 (chown -R 1000:1000 /mnt/pool/apps/plextrakt) before first run
    volumes:
      - /mnt/pool/apps/plextrakt:/config
    ports:
      - "9308:9308"   # prometheus /metrics
    restart: unless-stopped
